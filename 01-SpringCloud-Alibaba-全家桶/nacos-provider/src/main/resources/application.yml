spring:
  application:
    name: nacos-provider
  cloud:
    nacos:
      discovery:
        server-addr: 10.4.62.239:8848

server:
  port: 8081

management:
  endpoints:
    web:
      exposure:
        include: "*"